<center><h1>接口文档</h1></center>



## 版本信息

版本号：0.0.4

更新日期：2024-12-31



## 通用说明

### 注意事项
所有的接口皆为 promise ，需要使用 await 或者 then 进行处理


### 响应格式

```json
{
  code: 0,
  data: null,
  message: 'success'
}
```



### 错误码说明

- 0 表示操作成功

- -1 表示操作失败

- -2 表示用户取消了操作



### 通用参数说明

无



## 主页面 API

获取当前IP地址以及归属地信息

- 事件名: `get-ip-info`

- 方向: 渲染进程 → 主进程

- 参数: 无

- 返回值:

    ```json
    {
      code: 0,
      data:{
        ip: 1.1.1.1,
      	location: 中国-北京市-海淀区
      },
      message: 'success'
    }
    ```





开始记录

- 事件名: `start-record`

- 方向: 渲染进程 → 主进程

- 参数: 无

- 返回值:

    ```json
    {
      code: 0,
      data: null,
      message: 'success'
    }
    ```



暂停记录

- 事件名: `pause-record`

- 方向: 渲染进程 → 主进程

- 参数: 无

- 返回值:

    ```json
    {
      code: 0,
      data: null,
      message: 'success'
    }
    ```



继续记录

- 事件名: `resume-record`

- 方向: 渲染进程 → 主进程

- 参数: 无

- 返回值:

    ```json
    {
      code: 0,
      data: null,
      message: 'success'
    }
    ```



停止记录

- 事件名: `stop-record`

- 方向: 渲染进程 → 主进程

- 参数: 无

- 返回值:

    ```json
    {
      code: 0,
      data: null,
      message: 'success'
    }
    ```



是否存在历史记录

- 事件名: `has-history`

- 方向: 渲染进程 → 主进程

- 参数: 无

- 返回值:

    ```json
    {
      code: 0,
      data: boolean,
      message: 'success'
    }
    ```



导出结果

- 事件名: `export-record`

- 方向: 渲染进程 → 主进程

- 参数: 无

- 返回值:

    ```json
    {
      code: 0,
      data: {
        filePath: string,
        counts: number,
      },
      message: 'success'
    }
    ```



后端向前端同步 IP 结果

- 事件名: `ip-updated`

- 方向:  主进程 → 渲染进程

- 参数:

    ```json
    {
      code: 0,
      data:{
        ip: 1.1.1.1,
      	location: 中国-北京市-海淀区
      },
      message: 'success'
    }
    ```

- 返回值:  无


## 设置窗口 API

测试代理可用性

- 事件名: `test-proxy`

- 方向:   渲染进程 → 主进程

- 参数:

    ```json
    {
      type: 0, // 0: 无代理 、 1: 系统代理 、 2: 自定义代理
      data:{
        protocol: 0,   // 0: SOCKS5 、 1: HTTP 、 2: HTTPS
        addr: string,
      	port: number,
        auth: {
          user: string,  // 非必须
        	pass: string   // 非必须
        }
      },
    }
    ```

- 返回值:

    ```json
    {
        code: 0,
        data: {
            available: true,         // 是否可用
            ip: '1.2.3.4',          // 测试时获取到的IP
            location: '中国 上海',    // IP归属地
        },
        message: 'success'
    }
    ```



保存代理设置

- 事件名: `save-proxy`

- 方向:   渲染进程 → 主进程

- 参数:

    ```json
    {
      type: 0, // 0: 无代理 、 1: 系统代理 、 2: 自定义代理
      data:{
        protocol: 0,   // 0: SOCKS5 、 1: HTTP 、 2: HTTPS
        addr: string,
      	port: number,
        user: string,  // 非必须
        pass: string   // 非必须
      },
    }
    ```

- 返回值:

    ```json
    {
      code: 0,
      data: null,
      message: 'success'
    }
    ```



测试数据接口可用性

- 事件名: `test-api-source`

- 方向:   渲染进程 → 主进程

- 参数:

    ```json
    {
      apiSource: 0, // 0: ipip.net 、 1: ipify 、 2: 美图 、 3: ip-api
    }
    ```

- 返回值:

    ```json
    {
        code: 0,
        data: {
            available: true,         // 是否可用
            ip: '1.2.3.4',          // 测试时获取到的IP
            location: '中国 上海',    // IP归属地
        },
        message: 'success'
    }
    ```



保存数据接口选择

- 事件名: `save-api-source`

- 方向:   渲染进程 → 主进程

- 参数:

    ```json
    {
      apiSource: 0, // 0: ipip.net 、 1: ipify 、 2: 美图 、 3: ip-api
    }
    ```

- 返回值:

    ```json
    {
        code: 0,
        data: null,
        message: 'success'
    }
    ```



保存请求配置

- 事件名: `save-request-options`

- 方向:   渲染进程 → 主进程

- 参数:

    ```json
    {
      times: 5, // number, 表示重复尝试次数
      interval: 500 // number, 表示请求时间间隔
    }
    ```

- 返回值:

    ```json
    {
        code: 0,
        data: null,
        message: 'success'
    }
    ```



## 数据类型说明

### 代理类型

- 0  -  无代理

- 1  -  系统代理

- 2  -  自定义代理



### 协议类型

- 0  -  SOCKS5

- 1  -  HTTP

- 2  -  HTTPS



### API来源类型

- 0  -  ipip.net
- 1  -  ipify
- 2  -  baidu
- 3  -  ip-api

